# Timestamp Tools

## What it does

- `ts iso`: copy the current ISO-8601 timestamp
- `ts unix`: copy the current Unix timestamp in seconds

## APIs used

- `context.api.clipboard`
- `context.api.notification`

## Why this example exists

This example shows how to expose multiple features from a single plugin and route behavior by `featureCode`.
