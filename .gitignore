node_modules
dist
packages/mulby-cli/
skills/
.DS_Store
*.log
.claude
.agent
release
.shared
.codex
.workflow
.workflows
!build/
build/**
!build/icon.icns
!build/icon.ico
!build/icon.png
!build/logo.png
!build/icons/
!build/icons/**
!build/entitlements.mac.plist
!build/nsis-installer.nsh
!build/tray_icon*.png
# Native addon build artifacts — regenerated per-platform by node-gyp
# (make/.mk/config.gypi on macOS, .vcxproj/.sln/.node on Windows).
# Never commit these; `pnpm install` rebuilds them via native postinstall.
native/build/

cs
package-lock.json
.tmp-*
.vscode
.superpowers
ztools
.cursor
scripts/codepage-backups/
